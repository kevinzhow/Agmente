interface:
  display_name: "Codex Local CLI E2E"
  short_description: "Run Agmente E2E with local Codex CLI"
  default_prompt: "Run Agmente Codex local CLI E2E against a real app-server endpoint, execute only the Codex UI test, report pass/fail and root cause, and always cleanup server and simulator app state."
